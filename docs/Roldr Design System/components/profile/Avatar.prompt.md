Player / GM avatar. No photo → initials on the brand gradient (never a grey placeholder).

```jsx
<Avatar name="Marta Ruiz" size={56} ring live />
<Avatar src={player.photo} name={player.name} />
```

`ring` = table's GM. `live` = connected now.
