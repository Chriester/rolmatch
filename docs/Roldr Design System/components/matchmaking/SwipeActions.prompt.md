The action row under the swipe deck. Join is the gradient circle and is always the largest.

```jsx
<SwipeActions onNo={pass} onSuper={save} onYes={join} />
```

Press state is scale .92 with `--ease-dice` (the one overshoot easing in the system).
