Shown once, over the deck, when a table match happens. The only place a scale-from-0.6 pop is allowed.

```jsx
{matched && <MatchBanner title="La Tumba de Aniquilación" gm="Marta R." onOpen={open} onKeep={dismiss} />}
```
