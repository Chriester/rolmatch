Text field for search, sign-in and table setup.

```jsx
<Input label="Buscar mesa" icon="search" placeholder="Sistema, ciudad o etiqueta" />
<Input label="Correo" type="email" error="Ese correo ya tiene cuenta" />
```

Focus is a 2px `--focus-ring` (lilac 55%) box-shadow, never a colour change of the fill.
